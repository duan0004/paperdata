# T3.12 PRL Formal Compile Report

**Date**: 2026-04-23  
**Route**: PRL-first  
**Engine**: Tectonic 0.16.9.

## Outputs

| File | Pages | Size |
|---|---:|---:|
| `theory/pdf/revtex/paper_prl_submission.pdf` | 4 | 126697 bytes |
| `theory/pdf/revtex/prl_supplement.pdf` | 8 | 100787 bytes |

## Commands

```bash
cd theory
tectonic --keep-logs --keep-intermediates --outdir pdf/revtex paper_prl_submission.tex
tectonic --keep-logs --keep-intermediates --outdir pdf/revtex prl_supplement.tex
```

## Log Status

- Main text: compile succeeds after adding the PRL-framing cleanup, sequential
  anchored bridge interpretation, literature-positioning paragraph, and bridge
  evidence figure.  No
  overfull boxes, undefined citations, missing figures, or LaTeX errors were
  found in the log scan.  Remaining TeX log messages are underfull boxes.
- Supplement: compile succeeds after adding the full bridge model list,
  sequential bridge ablation, tested-representative family-mixture wording,
  PTArcade/ceffyl version and plateau diagnostics, robustness,
  gamma-projection, and frequency-cut diagnostics.  No overfull boxes,
  undefined citations, missing figures, or LaTeX errors were found in the log
  scan.  Remaining TeX log messages are underfull boxes.
- BibTeX warnings appear because the draft uses inline `thebibliography`
  entries rather than an external `.bib` database; these warnings did not stop
  PDF generation.

## Remaining Compile-Adjacent Work

1. Visually inspect the final PDFs after the bridge-framing revision.
2. If APS production prefers BibTeX, convert the inline `thebibliography` block
   into a `.bib` file before final upload.
3. Static gate after bridge-framing revision passes with
   `structural_pass=True` and `submission_ready=True`.
