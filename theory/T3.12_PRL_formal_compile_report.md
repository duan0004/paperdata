# T3.12 PRL Formal Compile Report

**Date**: 2026-04-22  
**Route**: PRL-first  
**Engine**: Tectonic 0.16.9, installed via Homebrew during this session.

## Outputs

| File | Pages | Size |
|---|---:|---:|
| `theory/pdf/revtex/paper_prl_submission.pdf` | 3 | 98389 bytes |
| `theory/pdf/revtex/prl_supplement.pdf` | 5 | 81803 bytes |

## Commands

```bash
tectonic --keep-logs -o theory/pdf/revtex theory/paper_prl_submission.tex
tectonic --keep-logs -o theory/pdf/revtex theory/prl_supplement.tex
```

## Log Status

- Main text: compile succeeds after the reviewer-risk PRL cleanup.  No
  overfull boxes, undefined citations, missing figures, or LaTeX errors were
  found in the log scan.  Remaining TeX log messages are underfull boxes.
- Supplement: compile succeeds after adding the prior/model-selection table and
  clarifying the two QMC/TI residual conventions.  No overfull boxes,
  undefined citations, missing figures, or LaTeX errors were found in the log
  scan.  Remaining TeX log messages are underfull boxes.
- BibTeX warnings appear because the draft uses inline `thebibliography`
  entries rather than an external `.bib` database; these warnings did not stop
  PDF generation.

## Remaining Compile-Adjacent Work

1. Visually inspect the final PDFs after the public code URL is inserted.
2. If APS production prefers BibTeX, convert the inline `thebibliography` block
   into a `.bib` file before final upload.
3. Re-run the static gate after adding a Zenodo DOI, if the final submission
   requires one.
